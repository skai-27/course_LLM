# 02. Tools — LLM이 호출할 수 있는 함수

## 학습 목표

- **Tool**이 “스키마가 있는 원격 함수 호출”에 가깝다는 점을 이해한다.
- 타입 힌트가 **인자 스키마**로 이어진다는 것을 안다.
- 읽기 전용 조회와 **부작용 있는 작업**을 나누는 설계 관점을 익힌다.

## 개념

**Tool**은 클라이언트(보통 LLM 에이전트 루프)가 `tools/call`로 **이름과 인자**를 넘기면, 서버가 Python 함수를 실행하고 **결과를 구조화**해 돌려주는 기능입니다.

- 계산, API 호출, 파일 쓰기, 티켓 생성 등 **실행·변경**이 수반되는 작업에 적합합니다.
- “그냥 문서 내용을 보여 주면 되는가?”라면 **Resource**가 더 단순할 수 있습니다.

## 프로토콜 관점(요약)

1. 서버가 도구 목록과 **JSON Schema 형태의 입력 정의**를 노출
2. 클라이언트가 도구 이름 + 인자로 호출
3. 서버가 결과(텍스트·구조화 데이터 등)를 반환

FastMCP는 함수 시그니처에서 스키마를 생성하므로, **인자 이름·타입·기본값**이 곧 API 계약이 됩니다.

## FastMCP 사용 요령

### 1) `@mcp.tool`

```python
@mcp.tool
def add(a: int, b: int) -> int:
    ...
```

- 함수 이름 → 도구 이름(기본)
- docstring → 설명(기본)
- 매개변수 → 필수/선택, 타입 힌트 반영

### 2) async

네트워크·DB 등 I/O가 있으면 `async def`를 사용합니다.

### 3) Context (선택)

로깅, 진행 알림, 클라이언트 기능 연동 등은 `Context`를 주입해 처리할 수 있습니다. ([Context 문서](https://gofastmcp.com/v2/servers/context))

## 설계 팁

- **작은 도구 여러 개**가 거대한 만능 도구 하나보다 디버깅과 권한 통제에 유리한 경우가 많습니다.
- 위험한 작업은 클라이언트 쪽 **사용자 승인( human-in-the-loop )** 과 함께 쓰는 경우가 많습니다(MCP 자체보다 **제품 정책** 영역).

## 예제 코드

[`examples/example_tools.py`](../examples/example_tools.py)

```bash
python examples/example_tools.py
```

## 체크리스트

- [ ] 인자 타입이 스키마에 반영되는지 확인했다.
- [ ] Resource로도 될 “조회”를 Tool로만 몰아넣지 않았는지 점검했다.
- [ ] (선택) `async` 도구로 외부 I/O를 시뮬레이션해 본다.
